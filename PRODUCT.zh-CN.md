# V2UI 产品基线

<p align="right"><a href="./PRODUCT.md">English</a> | <strong>中文</strong></p>

## 产品定位

V2UI 是面向 Codex 生成网站的**预览评审层**。它会保留用户所在页面、可见内容、选择或绘制的区域、语音内容和每个事件发生的时间。Codex 根据这些证据解析源码影响范围、说明潜在影响、等待批准、实现已确认的调整并验证结果。

V2UI 不是通用网页编辑器。它不会把运行时 DOM 当作源码，也不会在保存反馈时直接修改代码。

## 核心流程

1. 启动网站本地预览和 V2UI companion。
2. 在 Codex 内置浏览器或 Chrome 中打开同一评审层。
3. 用户开始评审，并明确授予屏幕或麦克风权限。
4. 浏览真实页面；使用 Select 或 Pen 标明视觉证据，同时通过语音描述建议。
5. 暂停时不丢失证据；恢复后向同一建议追加新的录音片段。
6. 将评审包保存到被评审项目。
7. 返回当前 Codex 任务。Codex 概括建议，并识别可能影响的实例、列表项、共享组件、设计 token、响应式规则或全局样式。
8. 仅在用户明确确认范围后修改、构建并验证代码。
9. 交付后开始干净的新一轮评审，持续迭代直至结果被接受。

## 两个评审表面，一个产品

- **Codex 内置浏览器：**仅用于开发的适配器会在 URL 包含 `?v2ui=1` 时注入共享浮层。
- **外部 Chrome：**Manifest V3 扩展会把同一浮层注入已授权的本地预览。

两个表面共用 companion、交互模型、评审 schema、存储位置和确认规则，不应演变成两个独立产品。

## 证据与建议模型

- `manifest.json` 是 DOM 目标、画笔标注、语音建议、时间戳、视口、URL、滚动位置和权限状态的结构化事实来源。
- 屏幕录制是辅助证据。DOM 选择只能证明用户指出了哪个运行时实例。
- 一条建议代表一个来自语音的调整请求，不等同于 DOM 目标数量。
- 新的视觉标记会关闭上一条建议的边界；短暂语音停顿继续归入当前建议。
- Chrome 在录制时即时显示 Web Speech 的中间与最终结果。停顿不会新建建议，新的 Select/Pen 标记才会。
- Codex 内置浏览器不依赖实时转录。每次暂停都向当前建议添加一段可播放音频；在创建新视觉标记前，后续片段继续归入同一建议。
- Undo 撤销一次视觉操作及仅与它关联的建议；Clear 清除当前轮次。
- 点击建议的 `×` 会完整丢弃其文本或音频、所有关联 Select/Pen 证据以及当前语音边界。新语音会创建新建议；延迟到达的识别结果不得恢复已删除内容或区域。
- 悬停建议时，高亮其关联组件、绘制区域或页面范围。

## 状态与降级能力

评审包含三个明确状态：录制中、已结束且可编辑、已发送并等待确认。停止录制与保存反馈是两个不同操作。

Web Speech 实时识别是尽力而为的能力，本地录音是独立证据路径。若语音服务不可用、网络失败或内置浏览器不支持实时识别，V2UI 必须继续录音、保留 DOM 与画笔证据、允许保存仅含音频的评审、在 manifest 中设置 `transcription.requiresPostProcessing`，并要求绑定的 Codex 任务转写录音、按视觉时间线对齐，不得仅根据标注猜测。

Chrome 与 Codex 共用浮层和 schema，但根据表面能力调整呈现：Chrome 优先实时文本，Codex 优先在暂停后展示可播放本地音频。Codex 的转录限制不得拖慢 Chrome 的实时建议。

屏幕、麦克风、录制和语音识别权限分别记录。缺少一种权限不得丢弃其他证据。

Codex Desktop 对打开的任务持有独占写入，外部 `codex exec resume` 可能因 `already has an active writer` 失败。因此默认流程不得承诺自动回调。companion 必须先可靠保存，再提供返回 Codex 的提示。实验性投递必须显式启用、保持只读，并且不得把已成功保存的评审显示为失败。

## 授权与影响范围

DOM 选择是证据，不是修改共享组件、全局 token、响应式规则、所有循环实例或跨页面样式的授权。Codex 写入前必须说明潜在源码影响。

**Save feedback**、**Send suggestions** 或 **Save and return to Codex** 仅授权本地保存与交接。用户明确确认概括后的范围之前，不得修改代码。

## 视觉与交互基线

- 使用低饱和暖橙/黄色和醒目但不刺眼的红色画笔。
- 工具条与建议面板使用不透明白色表面，不使用模糊或透明效果。
- 工具条显示暖橙色斜体 `V2UI` 字标，不显示图形 Logo。
- Chrome 扩展与 Codex 插件卡保留暖橙图形标记。
- 工具保持 Browse、Select、Pen、Undo、Clear 和 Exit review；浏览与标注模式严格分离。
- 绿色播放控制在就绪时显示播放，录制时显示暂停，旁边展示累计时间。
- 建议面板标题只显示状态点：录制时绿色，暂停或空闲时红色；标题可拖动面板。
- 首条建议出现后才显示建议面板；仅音频降级场景可在录制结束后展示保存操作。
- 首次引导与反馈使用屏幕居中的深棕色圆角 Toast；初次引导分为视觉平衡的两行。
- 保存成功后清空标注、建议、录音片段与累计时间，再开始下一轮。

## 安全与存储

- Chrome 使用 Manifest V3，仅申请 `activeTab` 和 `scripting`。
- 主机权限仅限 `http://localhost/*` 与 `http://127.0.0.1/*`。
- 只有用户开始评审后才请求屏幕与麦克风权限。
- Chrome 的已解压扩展必须由用户可见地加载；V2UI 不宣称支持静默安装。
- 每个被评审项目独立保存一次性 onboarding 状态；启动器可安全启动或复用 companion。
- 评审包只保存在 `<reviewed-project>/.codex/v2ui-reviews`。
- 运行状态与日志只保存在 `<reviewed-project>/.codex/v2ui`。
- 源码与发布包必须排除评审数据、onboarding 状态、录音、日志、安装缓存和其他产品源码。

## 发布基线

0.4.1 保留已验证的 0.3.1 双表面评审基础与 0.4.0 分发基线，包括项目 onboarding、`start-v2ui`、review-server companion、Vite 适配器、只读交接、屏幕/麦克风/DOM/画笔证据、弹性音频、Chrome 实时转录、Codex 可播放音频、完整建议删除、结构化 manifest 和干净轮次重置。产品 UI 与提交包保持英文，公开仓库文档提供英文与中文版本。
